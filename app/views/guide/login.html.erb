<%= form_with(model: nil, local: true) do |form| %>
  <div class="field">
    <%= form.label :name, 'Eメール' %>
    <%= form.text_field :name, required: true %>
  </div>

  <div class="field">
    <%= form.label :name, 'パスワード' %>
    <%= form.text_field :name, required: true %>
  </div>

  <div class="actions">
    <%= form.submit 'ログイン'%>
  </div>
<% end %>
